# =============================================================
# ServiceAccount used by the app Pod.
# The Vault Kubernetes auth method ties this ServiceAccount to a
# Vault role, so the injected Vault Agent can authenticate and
# pull secrets on the app's behalf.
# =============================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault-demo-app
  labels:
    app: vault-demo-app
